{{- if .Values.backend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "cs.backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cs.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
    {{- if .Values.backend.backendConfig.enabled }}
    cloud.google.com/backend-config: '{"default": "{{ include "cs.backend.fullname" . }}"}'
    {{- end }}
spec:
  type: {{ .Values.backend.service.type }}
  internalTrafficPolicy: Cluster
  selector:
    {{- include "cs.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
  ports:
    - name: {{ .Values.backend.service.portName }}
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
